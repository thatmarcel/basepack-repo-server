# Basepack Repo Server
**Modern server for repo.basepack.co**

## Running
You need to have [Rust tooling](https://www.rust-lang.org/tools/install) installed on your system.
Then, just run:
```bash
cargo run
```